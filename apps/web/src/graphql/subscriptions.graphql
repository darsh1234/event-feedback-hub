# Receive newly persisted feedback for the selected event.
subscription FeedbackAdded($eventId: ID!) {
  feedbackAdded(eventId: $eventId) {
    __typename
    id
    event {
      __typename
      id
      name
    }
    text
    rating
    createdAt
  }
}
